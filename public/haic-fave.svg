<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
  <!-- Transparent Background (so it adapts to any theme) -->
  
  <!-- White Border (Outline) - Slightly larger -->
  <!-- Vertical Bar Border -->
  <rect x="86" y="36" width="28" height="128" rx="2" fill="#FFFFFF"/>
  
  <!-- Horizontal Bar Border -->
  <rect x="36" y="86" width="128" height="28" rx="2" fill="#FFFFFF"/>
  
  <!-- Gold Plus (Inner) -->
  <!-- Vertical Bar -->
  <rect x="90" y="40" width="20" height="120" rx="2" fill="#d4af37"/>
  
  <!-- Horizontal Bar -->
  <rect x="40" y="90" width="120" height="20" rx="2" fill="#d4af37"/>
</svg>
